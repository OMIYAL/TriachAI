# 🚀 TriArchV5 — v2026.7.4.1046 (4 July 2026)

## ✨ New

**Browse upcoming events without logging in**
The public storefront can now show a list of upcoming events — sorted by date — to visitors who haven't signed in. No account is needed to see what's coming up.

**Message applicants directly from a service request**
Staff reviewing a service request can now click "Message Applicant" to jump straight into a chat with the person who submitted it. This button used to be disabled and marked "Coming Soon" — it's now fully working.

**New Service Catalog and Events Calendar widgets for your public pages**
You can now add two new content blocks to any public page: a browsable catalog of your services (grid or carousel view, filterable by category, with a "Start request" button) and an events calendar (weekly, monthly, or list view, filterable by type). Both received a visual refresh shortly after launch for cleaner, more consistent styling.

**Self-service Media Library for storefront images**
Admins managing the public storefront can now upload images and copy a shareable link to use in blog posts and pages, without needing a developer to place image assets manually. A follow-up update also cleaned up the page's layout and added clearer upload error messages.

## 💪 Improvements

**Control Room redesign: stickier navigation, side panels, cleaner lists**
Across Contacts, Fee Schedules, Request Forms, Permit Projects, Service Definitions, Service Requests, and Invoices, the page header now stays visible while you scroll, a consistent "back" button appears on create and edit screens, several edit forms now open as slide-out panels instead of popups, and list tables show clearer titles alongside an "Updated" date. New filters were added too, including Jurisdiction on Service Requests and Permit Projects.

**Faster Service Request and Permit Project lists on the storefront**
These lists now load, search, sort, and page without a full page reload, and the page header stays in view while you scroll.

**Reworked storefront experience for service requests and permit projects**
The Service Request detail page now leads with a simple status tracker and "what's next" guidance instead of a technical status diagram. Status and correction history have moved into a slide-out timeline, and your reports, letters, and certificates are grouped clearly with duplicates no longer showing twice. You'll now see a warning if you try to leave a permit project or application with unsaved changes, and the Permit Project edit page has a working "Back" button. Postal code fields now automatically strip out non-numeric characters as you type.

**Document editor gets a full toolbar**
The document editor now has a complete, tabbed toolbar (Home, Insert, Layout) instead of a stripped-down single row. Fullscreen mode now properly fills the screen, and dialogs like Rename, Move, Share, and Insert Table/Image no longer disappear while in fullscreen.

**See a reviewer's role when assigning them**
The reviewer picker now shows each person's role next to their name, making it easier to pick the right person for the job.

**Branded, consistent notification emails**
Outgoing notification emails now share one consistent layout and style, instead of looking inconsistent from message to message.

**Clearer validation when adding or editing contacts**
Entering an invalid phone number now shows an inline error message right away instead of a confusing failure. "No" answers on submission checklists are now highlighted so they stand out during review.

**Permit projects: safer sharing, clearer status, working invites**
You now only ever see your own projects. Inviting a contact to a project by email now actually sends the invite (previously the option didn't do anything). City and state fields now only accept letters. Clicking "Create project" now activates the project right away, while "Save draft" correctly keeps it as a draft — previously both buttons behaved the same way.

**Certificate step limited to certificate-issuing services**
The "Issuance" step can no longer be added to a service that isn't set up to produce a certificate, whether you're building the service template or configuring an individual request.

**Project document upload limit standardized to 100 MB**
Project document uploads are now capped at 100 MB per file (previously up to 200 MB), bringing this upload point in line with the limit used everywhere else in the app.

**More reliable request routing during setup**
Editing a request's steps before its review formally begins now reliably updates how the request will be routed, fixing a case where an edit at that stage could leave the workflow working from outdated information.

## 🐞 Fixes

**Project address no longer shows blank**
The Project Address field on a Service Request's detail page now correctly displays the address from the linked project instead of appearing empty.

**Draft applications no longer disappear from "My Requests"**
Draft service requests submitted through the storefront now correctly show up in your request list — a bug had been hiding them.

**Certificates: approval no longer closes a request too early**
Approving a certificate on a request that still had further steps configured no longer force-closes the whole case — it now correctly continues on to the remaining steps. The "Download Certificate" link on the public site also now appears only once the request is fully closed.

**Fixed a crash when creating certificate-issuing services**
Creating or updating a service definition set to produce a certificate no longer fails with an error — the required certificate step is now added successfully every time.

**Account invite and password reset links now work correctly**
Invite and password-reset emails for staff accounts previously could contain a broken link. They now correctly point to the right sign-in page.

**Fixed the "back to review" link losing track of your file**
When stepping through Review → Report → Verify and back again, the "back" link now correctly returns to the same document you were working on, instead of jumping to a different, more recently uploaded file.

**Reports and certificates no longer appear before a request is finished**
Reports, letters, and certificates on the public service request page now only appear once a request reaches a final status (Closed, Rejected, or Correction Required) — previously they could show up while the request was still under active review.

**Fixed a text-configuration issue affecting storefront pages**
Resolved an invalid entry in the app's text and label configuration that could have caused some page text to fail to load correctly.

---
_Generated by [Claude Code](https://claude.ai/code)_
